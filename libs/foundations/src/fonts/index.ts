const fonts = {
  heading: `'Inter', sans-serif`,
  body: `'Inter', sans-serif`,
};

export const fontSizes = ['xs', 'sm', 'md', 'lg', 'xl', '2xl'] as const;
export const fontWeights = ['normal', 'medium', 'semibold', 'bold'] as const;

export default fonts;
