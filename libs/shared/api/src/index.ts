// We're using redux toolkit query
// Here it goes the createApi
// and we have a folder with files for each of the "features"
// e.g: src/features/projects.ts -> GET|PATCH|DELETE /projects/ /projects/:id ...
